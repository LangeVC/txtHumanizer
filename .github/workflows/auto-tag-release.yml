name: Auto Tag Release (DISABLED)

# DISABLED 2026-07-28 (LVC-MIR-G0). Forgejo (git.langevc.com) is canonical; GitHub is
# a read-only mirror. This workflow previously bumped capability.yaml, committed and
# pushed the change, and created a tag + release ON GITHUB — a writeback into the
# mirror that is no longer allowed. Tags, version bumps and releases now originate
# ONLY on Forgejo. Create the tag on Forgejo; the mirror pushes it to GitHub where
# release.yml builds the release artifacts.
#
# Kept as a tombstone (no push/tag trigger; the single job is hard-disabled with
# 'if: false') so the history and intent are visible.

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is intentionally disabled. Create tags/releases on Forgejo."
          exit 1
